# Ansible configuration for the HerdR Engineering fleet playbooks.
# Run from this directory:
#   cd ansible && ansible-playbook -i inventory/hosts.yml playbooks/site.yml
[defaults]
roles_path = ./roles
inventory = ./inventory
deprecation_warnings = False
