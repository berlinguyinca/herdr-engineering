# Live validation evidence — 2026-09-24 (host: bender)

## Fleet reachability (herdr --machine)
beast: REACHABLE
fry: unreachable (error: unknown machine 'fry'; use `herdr machine list`)
mac: unreachable (error: unknown machine 'mac'; use `herdr machine list`)

## Dev fabric end-to-end drill (spec 0110)
1. python3 -m http.server 18777  (demo app)
2. herdr-eng devfabric register --machine bender --host 127.0.0.1 --port 18777 --label dev-fabric-demo
   -> lease_9bfe7257bdf94fa4 external_port=18000
3. herdr-eng devfabric serve lease_9bfe7257bdf94fa4  (binds 18000, proxies to 18777)
4. curl http://127.0.0.1:18000/ -> HTTP 200 '<h1>dev fabric demo</h1>'

## Browser preview (spec 0070)
herdr-eng web --port 8787 (started)
herdr-eng browser http://127.0.0.1:8787 --json
   -> ok=true, console_errors=[], artifact=art_ed96509c7f0e4cbb8c58 (PNG published to artifact workspace)

## Beast live snapshot (via herdr --machine beast api snapshot)
7 workspaces with live pi agent sessions (working/idle), protocol 22, herdr 0.9.1

## Tailnet-interface dev fabric (cross-device reachability, spec 0110)
Forwarder bound to bender's tailnet IP (NOT 0.0.0.0, NOT public):
  ss: LISTEN 100.104.39.6:18000  (herdr-eng)
curl http://100.104.39.6:18000/ -> HTTP 200 '<h1>dev fabric demo</h1>'
Any tailnet device (beast, iphone171, ipad157) can open that address.

---
Fabric gateway live drill (2026-09-24, host bender)
---
gateway: herdr-eng devfabric gateway --control-port 29999
  control: 100.104.39.6:29999 (tailnet only; loopback view via same local addr)
  router: loopback + tailnet per-lease binds
healthz: {"bound_ports": [], "ok": true, "url_base": "bender.tail0c50da.ts.net"}

register: HERDR_ENGINEERING_FABRIC_URL=http://100.104.39.6:29999
  herdr-eng devfabric register --machine bender --host auto --port 18999 --label final-drill
  → external_port 18000, url http://bender.tail0c50da.ts.net:18000, target 100.104.39.6:18999
  (bound immediately: first curl succeeded 0.4s after register)

front door:
  curl http://bender.tail0c50da.ts.net:18000/ → "<h1>dev fabric drill</h1>" (HTTP 200)
  curl http://100.104.39.6:18000/            → "<h1>dev fabric drill</h1>" (HTTP 200)

restart rebind:
  stopped gateway; restarted; healthz showed bound_ports [18001] auto-rebound
  from the store (lease registered by the previous instance, still active)

dead-target expiry:
  app (pid 801478) killed at 20:01:38Z
  PORT 18000 RELEASED after 76s (probe 15s + TTL 90s from last renewal;
  unbind joins accept threads so the port is fully released)

Bugs found/fixed during the drill (all with regression tests):
  - unbind leaked one connection through in-flight accept() → join threads
  - probe loop never released expired ports (active_leases() consumes the
    reconcile) → router self-sync each cycle
  - gateway_url_from_config ignored HERDR_ENGINEERING_FABRIC_URL → env wins
  - store writes from overlapping processes lost → reload_if_changed
  - two gateways could share one store → gateway.lock (pid:nonce)

---
Powerpack up/down (upgrade/rollback) live drill (2026-09-24, host bender)
---
herdr-eng powerpack status            → enabled: []
herdr-eng powerpack snapshot clean    → ok (captures lock + config + plugin_policy.json)
herdr-eng powerpack enable plannotator → UP: enabled ['plannotator']
herdr-eng powerpack rollback clean    → DOWN: enabled [] (policy restored)
Bug found+fixed: rollback crashed on str paths and did not restore the plugin
policy file (the enable/disable half of the drill). Now accepts path/~path/tag
and snapshots always capture the policy (empty {} = "nothing enabled").

---
Live pytest test-tree run (2026-09-24, host bender)
---
herdr-eng tests run pytest --repo berlinguyinca/herdr-engineering \
  --worktree 0110-transparent-dev-fabric --machine localhost \
  --workdir ~/IdeaProjects/herdr-engineering --selector tests/test_naming.py
→ run.started → 10x test.finished (all "passed") → run.finished
  result: {"status": "passed"}
Proves the TestEvent contract end-to-end on a real run (spec 0120, pytest row).

---
Live Woodpecker CI validation (2026-09-24, operator token provided)
---
Instance: ci.metabolomics.us — Woodpecker 3.18.1 behind OAuth2 Proxy v7.6.0
  (nginx/1.20.1). Auth: Bearer user JWT (type=user, user-id=1), machine-local
  ~/.config/herdr-engineering/ci-token (chmod 600; never in repo).

API discovery (the earlier /api/v0/ assumption was WRONG):
  - Real base is /api/ (NOT /api/v0/). Trailing slashes 301 -> SPA HTML, which
    is what originally misled the "v0" mis-diagnosis.
  - Repos are addressed by NUMERIC id: GET /api/repos, /api/repos/{id},
    /api/repos/{id}/pipelines, /api/repos/{id}/pipelines/{number}.
  - 3.x pipeline detail exposes workflows[].children[] (tasks), not steps[].
  - No REST log endpoint (all /.../log variants return the SPA shell); logs
    stream over WebSocket, and the OAuth2 proxy does NOT forward a bearer
    token on the /ws/ upgrade (returns the sign-in page). => ci logs/deubg
    degrade honestly and link the web UI.
  - /api/agents -> runner state (8 agents); raw payload includes each agent's
    auth token => stripped in the view.

Live commands (all via `herdr-eng ci ...`):
  ci repos      -> 23 repos (e.g. berlinguyinca/autospec id 17)
  ci agents     -> 8 runner agents (whiteale-kvm-node-*), token NOT leaked
  ci pipelines --repo berlinguyinca/autospec
                -> 11 pipelines; #12..#8 failure, #5 success, #4 canceled
  ci pipeline --repo berlinguyinca/autospec 12
                -> status failure, 15 tasks; FAILED rust-validate (exit 2),
                   FAILED rust-workspace-test (exit 101)
  ci logs --repo berlinguyinca/autospec --pipeline 12 --step rust-validate
                -> {log: null, note: WebSocket-only / web-UI link} (honest)
  ci debug --repo berlinguyinca/autospec 12
                -> bounded debug-with-pi handoff: failing_step
                   "rust-validate, rust-workspace-test", exit codes, web UI link

Code corrected in herdr_engineering/ci.py to the real contract (base /api/,
numeric id resolution, workflows->tasks flattening, agents with token
stripping, honest step_log, debug_with_pi). tests/test_ci.py locks the
contract (13 tests). Full suite: 114 passed, ruff clean, CI green.

---
Real Ansible convergence — macOS fleet host `mac` (2026-09-24)
---
Host: mac.tail0c50da.ts.net (100.84.185.100, macOS, Darwin), SSH as the fleet
  user over the tailnet. Inventory updated: `macbook-m4` -> `mac` (real
  tailnet name); connection details in ansible/inventory/host_vars/mac.yml
  (gitignored).

Bugs found and fixed during the live run:
  1. macOS role brew check always exited 0, so `when: brew_check.rc == 0`
     passed and the homebrew tasks would fail on any Mac. Now gated on a
     brew_present fact (command -v + explicit /opt/homebrew and /usr/local
     prefixes) with an explicit report.
  2. site.yml forced become:true for non-root users; mac's sudo needs a
     password and every mac task is user-scoped. become is now overridable:
     -e herdr_engineering_become=false.
  3. Non-interactive SSH on macOS does not source the login PATH: brew,
     tailscale and pi live in /opt/homebrew/bin -> everything looked MISSING.
     Play-level environment now prepends /opt/homebrew/bin, /usr/local/bin
     and the fleet user's ~/.local/bin (herdr). Subtle: the environment also
     applies to the setup task, so ansible_env.PATH is captured WITHOUT
     ~/.local/bin — herdr checks then fail every run (non-idempotent).
     Fixed by adding $HOME/.local/bin via ansible_env.HOME.
  4. Tailscale report matched 'tailscale' in `tailscale status` output, which
     never contains that word (it lists peers) -> always "down". Now rc==0
     and non-empty stdout.

Run 1 (converge):  ok=17 changed=8  failed=0
  - Homebrew detected (7.0.6), vim/mc/btop installed, git + python@3.12 ensured
  - herdr 0.9.1 already correct (pinned version), pi 0.87.1 present
  - berlinguyinca/pi-engineering + berlinguyinca/autospec cloned to ~/
  - ~/.local/share/herdr-engineering{,/artifacts} + ~/.config/herdr-engineering
    created (0750); config.yaml: llm_endpoint: https://llm.metabolomics.us
Run 2 (idempotency): ok=17 changed=0 failed=0
Post-check on the machine: vim/mc/btop via brew, herdr 0.9.1, pi 0.87.1
  (@earendil-works/pi-coding-agent), tailscale status "up", both repos and
  config present. No pre-existing user config was modified.
Remaining: fry, beast, bender convergence (operator go-ahead per host).
